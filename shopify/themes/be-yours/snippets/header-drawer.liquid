{% comment %}
  Renders header drawer

  Accepts:
  - main_menu: {Object} Main menu
  - language_selector: {HTML} Shows on the screen as customers scroll up when true
  - country_selector: {HTML} Show filtering when true

  Usage:
  {% render 'header-drawer', main_menu: section.settings.menu %}
{% endcomment %}

{%- if section.settings.enable_language_selector and localization.available_languages.size > 1 -%}
  {%- capture language_selector -%}
    <noscript>
      {%- form 'localization', id: 'HeaderDrawerLanguageFormNoScript', class: 'localization-form' -%}
        <div class="localization-form__select">
          <p class="visually-hidden" id="HeaderDrawerLanguageLabelNoScript">{{ 'localization.language_label' | t }}</p>
          <select class="localization-selector link" name="locale_code" aria-labelledby="HeaderDrawerLanguageLabelNoScript">
            {%- for language in localization.available_languages -%}
              <option value="{{ language.iso_code }}" lang="{{ language.iso_code }}" {%- if language.iso_code == localization.language.iso_code %} selected{% endif %}>
                {{ language.endonym_name | capitalize }}
              </option>
            {%- endfor -%}
          </select>
          {% render 'icon', icon: 'caret' %}
        </div>
        <button class="button button--tertiary">{{ 'localization.update_language' | t }}</button>
      {%- endform -%}
    </noscript>
    <localization-form>
      {%- form 'localization', id: 'HeaderDrawerLanguageForm', class: 'localization-form' -%}
        <div class="no-js-hidden">
          <span class="visually-hidden" id="HeaderDrawerLanguageLabel">{{ 'localization.language_label' | t }}</span>
          <div class="disclosure">
            <button type="button" class="disclosure__button localization-form__select localization-selector" aria-expanded="false" aria-controls="HeaderDrawerLanguageList" aria-describedby="HeaderDrawerLanguageLabel" aria-label="{{ 'accessibility.language_selector' | t }}">
              {% render 'icon', icon: 'localization' %}
              <span class="label">{{ localization.language.endonym_name | capitalize }}</span>
              {% render 'icon', icon: 'caret' %}
            </button>
            <ul id="HeaderDrawerLanguageList" role="list" class="disclosure__list list-unstyled" hidden>
              <li class="disclosure__item" tabindex="-1">
                <a class="disclosure__link link disclosure__link--active focus-inset" href="#" hreflang="{{ localization.language.iso_code }}" lang="{{ localization.language.iso_code }}" aria-current="true" data-value="{{ localization.language.iso_code }}" aria-label="{{ 'accessibility.change_language' | t: language: localization.language.endonym_name }}">
                  {{ localization.language.endonym_name | capitalize }}
                </a>
              </li>
              {%- for language in localization.available_languages -%}
                {%- if language.iso_code != localization.language.iso_code -%}
                  <li class="disclosure__item" tabindex="-1">
                    <a class="disclosure__link link focus-inset" href="#" hreflang="{{ language.iso_code }}" lang="{{ language.iso_code }}" data-value="{{ language.iso_code }}" aria-label="{{ 'accessibility.change_language' | t: language: language.endonym_name }}">
                      {{ language.endonym_name | capitalize }}
                    </a>
                  </li>
                {%- endif -%}
              {%- endfor -%}
            </ul>
          </div>
          <input type="hidden" name="locale_code" value="{{ localization.language.iso_code }}"/>
        </div>
      {%- endform -%}
    </localization-form>
  {%- endcapture -%}
{%- endif -%}

{%- if section.settings.enable_country_selector and localization.available_countries.size > 1 -%}
  {%- capture country_selector -%}
    <noscript>
      {%- form 'localization', id: 'HeaderDrawerCountryFormNoScript', class: 'localization-form' -%}
        <div class="localization-form__select">
          <p class="visually-hidden" id="HeaderDrawerCountryLabelNoScript">{{ 'localization.country_label' | t }}</p>
          <select class="localization-selector link" name="country_code" aria-labelledby="HeaderDrawerCountryLabelNoScript">
            {%- for country in localization.available_countries -%}
              <option value="{{ country.iso_code }}" {%- if country.iso_code == localization.country.iso_code %} selected{% endif %}>
                {{ country.currency.iso_code }} {{ country.currency.symbol }}
              </option>
            {%- endfor -%}
          </select>
          {% render 'icon', icon: 'caret' %}
        </div>
        <button class="button button--tertiary">{{ 'localization.update_country' | t }}</button>
      {%- endform -%}
    </noscript>
    <localization-form>
      {%- form 'localization', id: 'HeaderDrawerCountryForm', class: 'localization-form' -%}
        <div class="no-js-hidden">
          <span class="visually-hidden" id="HeaderDrawerCountryLabel">{{ 'localization.country_label' | t }}</span>
          <div class="disclosure">
            <button type="button" class="disclosure__button localization-form__select localization-selector" aria-expanded="false" aria-controls="HeaderDrawerCountryList" aria-describedby="HeaderDrawerCountryLabel" aria-label="{{ 'accessibility.country_selector' | t }}">
              {%- unless section.settings.enable_language_selector and localization.available_languages.size > 1 -%}
                {% render 'icon', icon: 'localization' %}
              {%- endunless -%}
              <span class="label">{{ localization.country.name }} ({{ localization.country.currency.iso_code }} {{ localization.country.currency.symbol }})</span>
              {% render 'icon', icon: 'caret' %}
            </button>
            <ul id="HeaderDrawerCountryList" role="list" class="disclosure__list list-unstyled" hidden>
              <li class="disclosure__item" tabindex="-1">
                <a class="disclosure__link link disclosure__link--active focus-inset" href="#" aria-current="true" data-value="{{ localization.country.iso_code }}" aria-label="{{ 'accessibility.change_country' | t: country: localization.country.name }}">
                  {{ localization.country.name }} ({{ localization.country.currency.iso_code }} {{ localization.country.currency.symbol }})
                </a>
              </li>
              {%- for country in localization.available_countries -%}
                {%- if country.iso_code != localization.country.iso_code -%}
                  <li class="disclosure__item" tabindex="-1">
                    <a class="disclosure__link link focus-inset" href="#" data-value="{{ country.iso_code }}" aria-label="{{ 'accessibility.change_country' | t: country: country.name }}">
                      {{ country.name }} ({{ country.currency.iso_code }} {{ country.currency.symbol }})
                    </a>
                  </li>
                {%- endif -%}
              {%- endfor -%}
            </ul>
          </div>
          <input type="hidden" name="country_code" value="{{ localization.country.iso_code }}"/>
        </div>
      {%- endform -%}
    </localization-form>
  {%- endcapture -%}
{%- endif -%}

{%- assign animate_count = 1 -%}
<header-drawer>
  <details class="menu-drawer-container">
    <summary class="header__icon header__icon--menu focus-inset" aria-label="{{ 'sections.header.menu' | t }}">
      <span class="header__icon header__icon--summary">
        {% render 'icon', icon: 'hamburger' %}
        <svg class="icon icon-close" aria-hidden="true" focusable="false">
          <use href="#icon-close"></use>
        </svg>
      </span>
    </summary>
    <div id="menu-drawer" class="menu-drawer motion-reduce" tabindex="-1">
      <div class="menu-drawer__inner-container">
        <div class="menu-drawer__navigation-container">
          <drawer-close-button class="header__icon header__icon--menu medium-hide large-up-hide" data-animate data-animate-delay-{{ animate_count }}>
            <button type="button" class="drawer-close-button__button" aria-label="{{ 'accessibility.close_menu' | t }}">
              <svg class="icon icon-close" aria-hidden="true" focusable="false">
                <use href="#icon-close"></use>
              </svg>
            </button>
          </drawer-close-button>
          <nav class="menu-drawer__navigation" data-animate data-animate-delay-{{ animate_count }}>
            <ul class="menu-drawer__menu list-menu" role="list">
              {%- for link in main_menu.links -%}
                {%- liquid
                  # Same binding as the desktop panel (desktop-menu.liquid): a
                  # mega_menu_showcase block matched to this item by title. The
                  # drawer may render a different menu (menu_mobile) — items
                  # only match when titles are the same in both menus.
                  assign enable_drawer_showcase = false
                  if link.links != blank
                    assign link_title_handle = link.title | downcase | handle
                    for block in section.blocks
                      if block.type != 'mega_menu_showcase'
                        continue
                      endif
                      assign menu_title_handle = block.settings.menu_title | downcase | handle
                      if menu_title_handle == blank or menu_title_handle != link_title_handle
                        continue
                      endif
                      if block.settings.show_in_drawer
                        assign enable_drawer_showcase = true
                        assign showcase_block = block
                      endif
                      break
                    endfor
                  endif
                -%}
                <li>
                  {%- if enable_drawer_showcase -%}
                    <details>
                      <summary>
                        <span class="menu-drawer__menu-item list-menu__item animate-arrow focus-inset{% if link.active or link.child_active %} menu-drawer__menu-item--active{% endif %}">
                          {%- liquid
                            assign item_title = link.title | escape
                            if enable_highlight and highlight_labels contains item_title
                              render 'highlight-text', hl_enable: true, hl_input: item_title, hl_phrases: highlight_labels, hl_style: highlight_style, hl_animateWrap: true
                            else
                              echo item_title
                            endif
                            render 'icon', icon: 'arrow'
                            render 'icon', icon: 'caret'
                          -%}
                        </span>
                      </summary>
                      <div id="link-{{ link.title | handle }}" class="menu-drawer__submenu motion-reduce" tabindex="-1">
                        <div class="menu-drawer__inner-submenu">
                          <div class="menu-drawer__topbar">
                            <button type="button" class="menu-drawer__close-button focus-inset" data-close aria-label="{{ 'accessibility.close_menu' | t }}">
                              {% render 'icon', icon: 'arrow' %}
                            </button>
                            <a class="menu-drawer__menu-item" href="{{ link.url }}">{{ link.title | escape }}</a>
                          </div>
                          {%- render 'drawer-showcase', link: link, block: showcase_block -%}
                        </div>
                      </div>
                    </details>
                  {%- elsif link.links != blank -%}
                    <details>
                      <summary>
                        <span class="menu-drawer__menu-item list-menu__item animate-arrow focus-inset{% if link.active or link.child_active %} menu-drawer__menu-item--active{% endif %}">
                          {%- liquid
                            assign item_title = link.title | escape
                            if enable_highlight and highlight_labels contains item_title
                              render 'highlight-text', hl_enable: true, hl_input: item_title, hl_phrases: highlight_labels, hl_style: highlight_style, hl_animateWrap: true
                            else
                              echo item_title
                            endif
                            render 'icon', icon: 'arrow'
                            render 'icon', icon: 'caret'
                          -%}
                        </span>
                      </summary>
                      <div id="link-{{ link.title | handle }}" class="menu-drawer__submenu motion-reduce" tabindex="-1">
                        <div class="menu-drawer__inner-submenu">
                          <div class="menu-drawer__topbar">
                            <button type="button" class="menu-drawer__close-button focus-inset" data-close aria-label="{{ 'accessibility.close_menu' | t }}">
                              {% render 'icon', icon: 'arrow' %}
                            </button>
                            <a class="menu-drawer__menu-item" href="{{ link.url }}">{{ link.title | escape }}</a>
                          </div>
                          <ul class="menu-drawer__menu list-menu" role="list" tabindex="-1">
                            {%- for childlink in link.links -%}
                              <li>
                                {%- if childlink.links == blank -%}
                                  <a href="{{ childlink.url }}" class="menu-drawer__menu-item list-menu__item focus-inset{% if childlink.current %} menu-drawer__menu-item--active{% endif %}"{% if childlink.current %} aria-current="page"{% endif %}>
                                    {{ childlink.title | escape }}
                                  </a>
                                {%- else -%}
                                  <details>
                                    <summary>
                                      <span class="menu-drawer__menu-item list-menu__item animate-arrow focus-inset">
                                        {%- liquid
                                          echo childlink.title | escape
                                          render 'icon', icon: 'arrow'
                                          render 'icon', icon: 'caret'
                                        -%}
                                      </span>
                                    </summary>
                                    <div id="childlink-{{ childlink.title | handle }}" class="menu-drawer__submenu motion-reduce">
                                      <div class="menu-drawer__topbar">
                                        <button type="button" class="menu-drawer__close-button focus-inset" data-close aria-label="{{ 'accessibility.close_menu' | t }}">
                                          {% render 'icon', icon: 'arrow' %}
                                        </button>
                                        <a class="menu-drawer__menu-item" href="{{ childlink.url }}">{{ childlink.title | escape }}</a>
                                      </div>
                                      <ul class="menu-drawer__menu list-menu" role="list" tabindex="-1">
                                        {%- for grandchildlink in childlink.links -%}
                                          <li>
                                            <a href="{{ grandchildlink.url }}" class="menu-drawer__menu-item list-menu__item focus-inset{% if grandchildlink.current %} menu-drawer__menu-item--active{% endif %}"{% if grandchildlink.current %} aria-current="page"{% endif %}>
                                              {{ grandchildlink.title | escape }}
                                            </a>
                                          </li>
                                        {%- endfor -%}
                                      </ul>
                                    </div>
                                  </details>
                                {%- endif -%}
                              </li>
                            {%- endfor -%}
                          </ul>
                        </div>
                      </div>
                    </details>
                  {%- else -%}
                    <a href="{{ link.url }}" class="menu-drawer__menu-item list-menu__item focus-inset{% if link.current %} menu-drawer__menu-item--active{% endif %}"{% if link.current %} aria-current="page"{% endif %}>
                      {%- liquid
                        assign item_title = link.title | escape
                        if enable_highlight and highlight_labels contains item_title
                          render 'highlight-text', hl_enable: true, hl_input: item_title, hl_phrases: highlight_labels, hl_style: highlight_style, hl_animateWrap: true
                        else
                          echo item_title
                        endif
                      -%}
                    </a>
                  {%- endif -%}
                </li>
              {%- endfor -%}
            </ul>
          </nav>

          {%- if section.settings.drawer_promo_image != blank or section.settings.drawer_promo_heading != blank -%}
            {%- liquid
              assign animate_count = animate_count | plus: 1
              assign promo_alt = section.settings.drawer_promo_heading | escape
              assign promo_tag = 'div'
              if section.settings.drawer_promo_link != blank
                assign promo_tag = 'a'
              endif
            -%}
            <{{ promo_tag }}{% if promo_tag == 'a' %} href="{{ section.settings.drawer_promo_link }}"{% endif %} class="menu-drawer__promo focus-inset" data-animate data-animate-delay-{{ animate_count }}>
              {%- if section.settings.drawer_promo_image != blank -%}
                <div class="menu-drawer__promo-media">
                  {{ section.settings.drawer_promo_image | image_url: width: 900 | image_tag: loading: 'lazy', alt: promo_alt, widths: '450, 600, 900', sizes: '(min-width: 750px) 410px, calc(100vw - 8rem)' }}
                </div>
              {%- endif -%}
              {%- if section.settings.drawer_promo_heading != blank -%}
                <span class="menu-drawer__promo-heading">
                  <span>{{ section.settings.drawer_promo_heading | escape }}</span>
                  {%- if promo_tag == 'a' %}{% render 'icon', icon: 'arrow' %}{% endif -%}
                </span>
              {%- endif -%}
            </{{ promo_tag }}>
          {%- endif -%}

          {%- if language_selector != blank or country_selector != blank -%}
            <div class="menu-drawer__localization" data-animate data-animate-delay-{{ animate_count }}>
              {%- liquid
                echo language_selector
                echo country_selector
                
                assign animate_count = animate_count | plus: 1
              -%}
            </div>
          {%- endif -%}

          <div class="menu-drawer__utility-links" data-animate data-animate-delay-{{ animate_count }}>
           {%- if shop.customer_accounts_enabled -%}
            <div class="menu-drawer__account">
              <shopify-account {% if section.settings.customer_account_menu %} menu="{{ section.settings.customer_account_menu }}"{% endif %}>
                <div  class="link link-with-icon focus-inset" slot="signed-out-avatar">
                 {% render 'icon', icon: 'account' %}
                  <span class="label">
                    {%- liquid
                      if customer
                        echo 'customer.account_fallback' | t
                      else
                        echo 'customer.log_in' | t
                      endif
                    -%}
                  </span>
                </div>
              </shopify-account>
            </div>
           {%- endif -%}
            <ul class="list list-social list-unstyled" role="list">
              {%- render 'social-icons' -%}
            </ul>
          </div>
        </div>
      </div>
    </div>
  </details>
</header-drawer>